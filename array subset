class Solution {
public:
    int missingNum(vector<int>& arr) {
        int n = arr.size() + 1;

        int totalSum = n * (n + 1) / 2;
        int arrSum = 0;

        for (int num : arr) {
            arrSum += num;
        }

        return totalSum - arrSum;
    }
};
